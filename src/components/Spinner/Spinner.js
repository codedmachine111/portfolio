import {SpinnerOverlay, SpinnerContainer} from './spinner.styles';

export const Spinner = () => {
    return (
        <SpinnerOverlay>
        <SpinnerContainer />
        </SpinnerOverlay>
    );
}